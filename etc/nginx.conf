server {
  listen 80;
  
  location ^~ /hello/ {
  proxy_path http://0.0.0.0:8080;
  }
  location ^~ /uploads/ {
  root /home/box/web;
  }
  location ~ \.\w\w\w?\w?$ {
  root /home/box/web/public;
  }
  location / {
  return 404;
  }
}

